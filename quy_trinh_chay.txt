# 0 chọn patient_id (bệnh nhân) trong .env

# 1. Chạy trên terminal : python reload_server.py

# 2. Build (mở terminal mới)
docker-compose build

# 3. Chạy toàn bộ 
docker compose up -d 


# 4. Đợi api build xong Mở http://localhost:5000/dashboard -> chọn Patient ID -> apply (đợi spark-app load batch để hiện visual)

